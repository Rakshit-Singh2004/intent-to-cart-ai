# Base URL of the deployed backend API (must end with /api).
# Local dev does NOT need this — Vite proxies /api to http://localhost:3001.
# In production (Vercel), set this to your Render backend URL, e.g.:
# VITE_API_URL=https://intent-to-cart-backend.onrender.com/api
VITE_API_URL=
